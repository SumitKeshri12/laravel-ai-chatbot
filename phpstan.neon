includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:
    level: 7 # Highest level for strictest checks (you can also try 7 or 8 first)
    paths:
        - app
        - routes
        - database
        - tests
        - vendor

    excludePaths:
        - storage/*
        - bootstrap/*
        - vendor/*
        - database/migrations/*
        - database/factories/*
    ignoreErrors:
    scanFiles:

    scanDirectories:
